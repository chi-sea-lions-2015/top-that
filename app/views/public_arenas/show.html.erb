<%= @challenger_video.title %><br>
<div>
  <%= @challenger_video.data_content %>
</div>

<% if @public_arena.status == "in_battle" %>

    <%= @challenger_video.votes.count%>
    <%= link_to image_tag("arrow_up.gif", width: "50"), video_votes_path(@challenger_video, @vote), method: :post %><br>

    <%= link_to image_tag("arrow_down.gif", width: "50"), video_votes_path(@challengee_video, @vote), method: :post %><br>
    <%= @challengee_video.votes.count%>

<%end%>

<%if @challengee_video%>
  <%= @challengee_video.title %><br>
  <%= @challengee_video.data_content %><br>
<%end%>

<% if @public_arena.status == "open" %>
    <%= link_to 'Step into the Arena', edit_video_public_arena_path(@challenger_video,@public_arena) %>
<%end%>
